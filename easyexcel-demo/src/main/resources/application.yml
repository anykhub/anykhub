server:
  port: 8080

spring:
  application:
    name: easyexcel-demo
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

logging:
  level:
    root: INFO
    com.example.demo: DEBUG
